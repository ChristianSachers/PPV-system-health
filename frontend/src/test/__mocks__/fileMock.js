/**
 * File Mock for Static Assets
 *
 * Mock static file imports (images, fonts, etc.) for Jest testing
 * This prevents Jest from trying to load actual files during tests
 */

module.exports = 'test-file-stub'